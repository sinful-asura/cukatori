# Frontend nginx image (Angular static files) — production only.
# Local FE is `ng serve` on the host.

services:
  web:
    image: ${DOCKER_REGISTRY}/ascend-os-web:${IMAGE_TAG:-latest}
    container_name: ascend-os-web
    restart: unless-stopped
    ports:
      - '127.0.0.1:8080:80'
